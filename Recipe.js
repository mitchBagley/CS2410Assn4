//adding all input fields for the ingredients methods
const ingredient = document.getElementById("ingredient");
const button = document.getElementById("add-ingr-button");

/* button.addEventListener("input", () => 
{
    if (output.innerHTML.includes("1") || output.innerHTML.includes("2") || output.innerHTML.includes("3") || output.innerHTML.includes("4") ||
    output.innerHTML.includes("5") || output.innerHTML.includes("6") || output.innerHTML.includes("7") || output.innerHTML.includes("8") || output.innerHTML("9"))
    {
        output.innerHTML = "No numbies and no nothing. Words only.";
    }
}); */
